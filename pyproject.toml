[project]
name = "cartographic-quiz"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "folium>=0.20.0",
    "geopy>=2.4.1",
    "pytest>=9.0.3",
    "requests>=2.34.2",
]

[tool.uv]
package = true

[project.scripts]
cartographic-quiz = "cartographic_quiz.__main__:main"
